<script>
    setTimeout(function () {
        document.querySelector(".success").remove();
    }, 3000)
</script>
<h1>Waiter App</h1>

<h1>Welcome, {{username}}!</h1>
{{!-- {{#each daysForWaiter }}
<li>{{this.days}}</li>
{{/each}} --}}
<p class="success">{{successMsg}}</p>

<form action="/waiters/{{username}}" method="POST">

   {{#each scheduleDays}}
    <label for="{{id}}">{{days}}</label>
    <input id="{{id}}" type="checkbox" name="checkday" class="check" value="{{id}}" {{#if checked}}checked{{/if}}>
{{/each}}

    <button type="submit">Update</button>
</form>

</body>

</html>